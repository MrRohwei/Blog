name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "voidpen-frontend/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: voidpen-frontend/package-lock.json

      - name: Build Frontend
        working-directory: ./voidpen-frontend
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL || 'https://yourdomain.com' }}
        run: |
          npm ci
          npm run build

      - name: Clean Remote Frontend Dist
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            mkdir -p /opt/voidpen/frontend/dist
            rm -rf /opt/voidpen/frontend/dist/*

      - name: Upload Frontend Dist
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "voidpen-frontend/dist/*"
          target: "/opt/voidpen/frontend/dist/"
          strip_components: 2
